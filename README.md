# SIR — Smart Interactive Reminder

The app lives in [`reminder-packager/`](reminder-packager/).

## Build a signed Android bundle
GitHub ▸ **Actions** ▸ **Android Release (signed AAB → Play)** ▸ **Run workflow**
(version name `1.0.0`, version code `1`). The signed `.aab` appears as a downloadable artifact.
